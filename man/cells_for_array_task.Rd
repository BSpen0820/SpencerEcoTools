% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Endotherm_Modeling.R
\name{cells_for_array_task}
\alias{cells_for_array_task}
\title{Distribute cells round-robin across a SLURM array}
\usage{
cells_for_array_task(
  valid_cell_indices,
  clust_array_arg = NULL,
  clust_array_size = NULL
)
}
\arguments{
\item{valid_cell_indices}{Integer vector of cell indices to distribute
(e.g. from \code{\link{read_valid_cell_indices}}).}

\item{clust_array_arg}{Integer. Value of \code{$SLURM_ARRAY_TASK_ID} for
this node (1-based). \code{NULL} (default) returns every cell — use this
when not running under a SLURM array.}

\item{clust_array_size}{Integer. Total number of array tasks. Required
when \code{clust_array_arg} is set.}
}
\value{
An integer vector, the subset of \code{valid_cell_indices} assigned
to this array task.
}
\description{
Mirrors the \code{clust_array_arg}/\code{clust_array_size} SLURM convention
used by \code{\link{run_micro_big_nichemap}}: the full set of cells is
enumerated and distributed round-robin across \code{clust_array_size} array
tasks, and only the current task's subset is returned.
}
